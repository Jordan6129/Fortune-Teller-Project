$("button").click(function(){ // when the button is clicked the function is activated //

    var career = $(".career").val(); // declaration of the variable "career" //
    
    var hours = parseInt($("#hours").val()); // declaring the variable "hours" and giving it the value of the input id "hours"//
    
    var profession = $(".profession").val; // declaration of the variale "profession" //
    
    var salary = parseInt(salary)*26;
    $("p").text("By the year 2045 you would have made"+salary+" dollars.");
});